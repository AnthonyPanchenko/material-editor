import BaseColorPicker from './BaseColorPicker';

class GradientBox extends BaseColorPicker {
  onMouseDown(event) {
    super.onMouseDown(event);
  }

  onMouseMove(event) {
    super.onMouseMove(event);

    // if (this.isMouseDown) {
    // }
  }
}

export default GradientBox;
