import BaseColorPicker from './BaseColorPicker';

class HueScale extends BaseColorPicker {
  onMouseDown(event) {
    super.onMouseDown(event);
  }

  onMouseMove(event) {
    super.onMouseMove(event);

    // if (this.isMouseDown) {
    // }
  }
}

export default HueScale;
